using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class followBoss : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
        //Vector3 target = new Vector3(transform.parent.gameObject.transform.position.x + 0.3f, transform.parent.gameObject.transform.position.y + 1f, transform.parent.gameObject.transform.position.z);
        //transform.position = Vector3.MoveTowards(transform.position, target, Time.deltaTime);
    }
}
